# Production: already provided by the Vercel ↔ Upstash integration
# (same vars as the previous Minesweeper deploy).
#
# Local: optional — copy from Vercel dashboard → Project → Settings → Environment Variables
# Without them, multiplayer rooms use in-memory storage for this process only.

UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
